import math
import sys
from os import rename

import requests

# (sys.version)
print(sys.executable)

print("Neil")


def greet(who_to_greet):
    greeting = "Hello, {}".format(who_to_greet)
    return greeting
